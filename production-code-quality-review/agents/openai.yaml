interface:
  display_name: "Production Code Quality Review"
  short_description: "Evidence-first production code review workflow with deterministic scope collection"
  default_prompt: "Use $production-code-quality-review to review this change for production safety, correctness, robustness, and merge readiness."
policy:
  allow_implicit_invocation: true
